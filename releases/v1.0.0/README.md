# releases/v1.0.0 — Release placeholder

This directory will contain the build outputs after running `python build.py`:

```
v1.0.0/
├── app_payload.zip                        ← PyInstaller dist zipped (web installer payload)
├── standalone_installer/
│   └── KiCadConfigurator_FullSetup_v1.0.0.exe
└── web_installer/
    └── KiCadConfigurator_WebSetup_v1.0.0.exe
```

Run `python build.py` from the repository root to generate these files.
